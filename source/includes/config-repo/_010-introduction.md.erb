# Config Repositories

## Current Version

This document is for version 2.0 of the Config Repositories endpoint.

## Introduction
Config repository extension endpoint allows a plugin to externalize pipeline and environment configuration. This allows the configuration to be maintained and versioned outside of GoCD.

<%= partial 'includes/shared/plugin-structure.md.erb', locals: { plugin_class: 'SimpleConfigRepositoryPlugin', plugin_type: 'configrepo', endpoint_version: '1.0', minimum_version: '16.8.0' } %>
