# Elastic Agents

Elastic Agent extension endpoint allows GoCD to interface with various cloud computing services to create agents on demand.

If you're looking to start away with a basic template for elastic agents, we recommend forking this [github repository](https://github.com/gocd-contrib/elastic-agent-skeleton-plugin).

<%= partial 'includes/shared/plugin_structure.md.erb', locals: { plugin_class: 'DockerElasticAgentPlugin', plugin_type: 'elastic-agent' } %>
