# Task Plugins

The Task extension endpoint allows plugin authors to extend GoCD to run new tasks in addition to the defaults that GoCD provides.

If you're looking to start away with a basic template for notification plugins, we recommend forking this [github repository](https://github.com/gocd-contrib/task-skeleton-plugin).

<%= partial 'includes/shared/plugin-structure.md.erb', locals: { plugin_class: 'EchoTaskPlugin', plugin_type: 'task', minimum_version: '15.1.0' } %>
