## Get Store Config Metadata

This is a message that the plugin should, to allow users to configure global artifact stores from the Artifact Stores View in GoCD.

<p class='request-name-heading'>Request name</p>

`cd.go.artifact.store.get-metadata`

<p class='request-body-heading'>Request body</p>

The server will not provide a request body.

<p class='response-code-heading'>Response code</p>

The plugin is expected to return status `200` if it can understand the request.

<p class='response-body-heading'>Response Body</p>

> An example response body for a docker plugin

```json
[
  {
    "key": "RegistryURL",
    "metadata": {
      "required": true,
      "secure": false
    }
  },
  {
    "key": "Password",
    "metadata": {
      "required": true,
      "secure": true
    }
  }
]
```

A JSON [metadata object](#the-metadata-object).